---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-deployment
  labels:
    app: dev
spec:
  replicas: 3
  selector:
    matchLabels: 
      app: dev
  template: 
    metadata:
      name: my-pod
      labels:
        app: dev
    spec:
      containers:
        - name: my-pod
          image: httpd
          ports:
            - containerPort: 80
      
...
---

apiVersion: v1 
kind: Service
metadata:
  name: my-svc
  labels: 
    app: dev
spec:
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  type: NodePort
  selector:
    app: dev

...