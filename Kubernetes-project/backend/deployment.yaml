---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-deployment
  labels:
    app: three-tier
spec:
  replicas: 3
  selector:
    matchLabels:
      app: three-tier
  template:
    metadata:
      labels:
        app: three-tier
    spec:
      containers:
        - name: backend-server
          image: mehulbisen/mehul-repo:backendff
          ports:
            - containerPort: 8080
            
  strategy: 
    type: RollingUpdate

...

---
apiVersion: v1
kind: Service
metadata: 
  name: my-service
  labels:
    app: three-tier
spec:
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  type: NodePort
  selector:
    app: three-tier

...



